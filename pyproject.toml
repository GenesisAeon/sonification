[project]
name = "genesisaeon-sonification"
version = "1.0.0"
description = "Data sonification layer for the GenesisAeon stack -- entropy waves to sine tones, UTAC thresholds to pitches, mandala resonance to rhythms."
readme = "README.md"
license = { text = "GPL-3.0-or-later" }
authors = [{ name = "GenesisAeon Team", email = "team@genesisaeon.org" }]
requires-python = ">=3.11"
keywords = ["sonification", "audio", "entropy", "midi", "genesisaeon", "data-to-sound"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Multimedia :: Sound/Audio",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "numpy>=2.0.0",
    "scipy>=1.14.0",
    "matplotlib>=3.8.0",
    "midiutil>=1.2.1",
    "rich>=13.0.0",
    "typer>=0.15.0",
]

[project.optional-dependencies]
stack = [
    "mandala-visualizer>=1.0.0",
    "utac-core>=1.0.0",
    "sigillin>=1.0.0",
    "fieldtheory>=1.0.0",
    "cosmic-moment>=1.0.0",
    "medium-modulation>=1.0.0",
    "entropy-governance>=1.0.0",
    "entropy-table>=2.0.0",
    "implosive-genesis>=1.0.0",
]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.9.0",
    "mypy>=1.10.0",
    "pre-commit>=3.7.0",
]
docs = [
    "mkdocs>=1.6.0",
    "mkdocs-material>=9.6.0",
    "mkdocstrings[python]>=0.27.0",
]

[project.scripts]
soni = "sonification.cli:app"

[project.urls]
Repository = "https://github.com/GenesisAeon/sonification"
Issues = "https://github.com/GenesisAeon/sonification/issues"
Documentation = "https://genesisaeon.github.io/sonification"
DOI = "https://doi.org/10.5281/zenodo.21000416"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sonification"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "B", "I", "W", "UP", "C4", "SIM"]
ignore = ["B008"]  # typer uses function calls in defaults

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101"]  # allow assert in tests

[tool.mypy]
python_version = "3.11"
strict = true

[[tool.mypy.overrides]]
module = ["midiutil.*", "scipy.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=sonification --cov-report=term-missing -v"

[tool.coverage.run]
source = ["src"]
omit = ["tests/*"]

